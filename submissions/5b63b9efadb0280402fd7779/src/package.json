{
  "name": "haggle-solution",
  "version": "0.0.1",
  "description": "A solution to the hola haggle challenge",
  "scripts": {
    "build": "rm -rf build/ && tsc",
    "build:watch": "rm -rf build/ && tsc --watch",
    "lint": "tslint -p tsconfig.json"
  },
  "author": "Robbie Speed",
  "license": "ISC",
  "devDependencies": {
    "@types/node": "^10.3.5",
    "source-map-support": "^0.5.6",
    "tslint": "^5.10.0",
    "typescript": "^2.9.2"
  }
}
